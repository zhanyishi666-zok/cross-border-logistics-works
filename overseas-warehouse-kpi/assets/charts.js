/* Generated by Trae Work */
;(function () {
  'use strict';

  /* ---------- read CSS variables ---------- */
  var cs = getComputedStyle(document.documentElement);
  var C = {
    bg:      cs.getPropertyValue('--bg').trim(),
    bg2:     cs.getPropertyValue('--bg2').trim(),
    ink:     cs.getPropertyValue('--ink').trim(),
    muted:   cs.getPropertyValue('--muted').trim(),
    rule:    cs.getPropertyValue('--rule').trim(),
    accent:  cs.getPropertyValue('--accent').trim(),
    accent2: cs.getPropertyValue('--accent2').trim()
  };

  /* ---------- common helpers ---------- */
  var palette = [C.accent, C.accent2, '#34d399', '#fb923c', '#f472b6'];

  function textStyle () {
    return { color: C.muted, fontFamily: 'InstrumentSans, sans-serif' };
  }

  function axisLine () {
    return { lineStyle: { color: C.rule } };
  }

  function splitLine () {
    return { lineStyle: { color: C.rule, type: 'dashed' } };
  }

  function tooltipBase () {
    return {
      backgroundColor: C.bg2,
      borderColor: C.rule,
      textStyle: { color: C.ink, fontFamily: 'InstrumentSans, sans-serif' }
    };
  }

  /* ---------- 1. Daily In/Out Trend (line) ---------- */
  var days = [];
  for (var d = 7; d <= 20; d++) days.push('7.' + d);

  var inData  = [820, 915, 880, 950, 1020, 1080, 1150, 1250, 1180, 1300, 1350, 1280, 1420, 1452];
  var outData = [980, 1050, 1020, 1100, 1180, 1250, 1300, 1400, 1350, 1450, 1500, 1420, 1550, 1606];

  var trendChart = echarts.init(document.getElementById('chart-trend'));
  trendChart.setOption({
    animation: false,
    tooltip: Object.assign({}, tooltipBase(), { trigger: 'axis' }),
    legend: {
      data: ['入库量', '出库量'],
      textStyle: textStyle(),
      top: 0,
      right: 0
    },
    grid: { top: 36, left: 56, right: 24, bottom: 32 },
    xAxis: {
      type: 'category',
      data: days,
      axisLine: axisLine(),
      axisLabel: textStyle(),
      axisTick: { show: false }
    },
    yAxis: {
      type: 'value',
      axisLine: { show: false },
      axisLabel: textStyle(),
      splitLine: splitLine()
    },
    series: [
      {
        name: '入库量',
        type: 'line',
        smooth: true,
        symbol: 'circle',
        symbolSize: 6,
        lineStyle: { width: 2, color: C.accent },
        itemStyle: { color: C.accent },
        areaStyle: {
          color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
            { offset: 0, color: C.accent.replace(')', ',0.25)').replace('rgb', 'rgba') },
            { offset: 1, color: 'rgba(0,0,0,0)' }
          ])
        },
        data: inData
      },
      {
        name: '出库量',
        type: 'line',
        smooth: true,
        symbol: 'circle',
        symbolSize: 6,
        lineStyle: { width: 2, color: C.accent2 },
        itemStyle: { color: C.accent2 },
        areaStyle: {
          color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
            { offset: 0, color: C.accent2.replace(')', ',0.25)').replace('rgb', 'rgba') },
            { offset: 1, color: 'rgba(0,0,0,0)' }
          ])
        },
        data: outData
      }
    ]
  });

  /* ---------- 2. Warehouse Distribution (pie) ---------- */
  var pieChart = echarts.init(document.getElementById('chart-pie'));
  pieChart.setOption({
    animation: false,
    tooltip: Object.assign({}, tooltipBase(), { trigger: 'item', formatter: '{b}: {d}%' }),
    legend: {
      orient: 'vertical',
      right: 12,
      top: 'center',
      textStyle: textStyle(),
      itemWidth: 12,
      itemHeight: 12,
      itemGap: 14
    },
    color: palette,
    series: [{
      type: 'pie',
      radius: ['42%', '70%'],
      center: ['38%', '50%'],
      avoidLabelOverlap: false,
      label: {
        show: true,
        color: C.ink,
        formatter: '{d}%',
        fontSize: 13,
        fontFamily: 'GeistMono, monospace'
      },
      labelLine: { lineStyle: { color: C.rule } },
      emphasis: {
        label: { show: true, fontSize: 15, fontWeight: 'bold' },
        itemStyle: { shadowBlur: 20, shadowColor: 'rgba(0,0,0,0.4)' }
      },
      data: [
        { value: 38, name: '美西仓' },
        { value: 28, name: '美东仓' },
        { value: 18, name: '英仓' },
        { value: 16, name: '德仓' }
      ]
    }]
  });

  /* ---------- 3. Channel Outbound (bar) ---------- */
  var barChart = echarts.init(document.getElementById('chart-channel'));
  barChart.setOption({
    animation: false,
    tooltip: Object.assign({}, tooltipBase(), { trigger: 'axis' }),
    grid: { top: 24, left: 56, right: 24, bottom: 32 },
    xAxis: {
      type: 'category',
      data: ['FBA入仓', '一件代发', '暂存转运', '退件处理'],
      axisLine: axisLine(),
      axisLabel: textStyle(),
      axisTick: { show: false }
    },
    yAxis: {
      type: 'value',
      max: 50,
      axisLabel: Object.assign({}, textStyle(), { formatter: '{value}%' }),
      axisLine: { show: false },
      splitLine: splitLine()
    },
    series: [{
      type: 'bar',
      barWidth: '48%',
      data: [
        { value: 42, itemStyle: { color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [{ offset: 0, color: C.accent }, { offset: 1, color: '#0ea5e9' }]) } },
        { value: 35, itemStyle: { color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [{ offset: 0, color: C.accent2 }, { offset: 1, color: '#6366f1' }]) } },
        { value: 15, itemStyle: { color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [{ offset: 0, color: '#34d399' }, { offset: 1, color: '#10b981' }]) } },
        { value: 8,  itemStyle: { color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [{ offset: 0, color: '#fb923c' }, { offset: 1, color: '#f97316' }]) } }
      ],
      label: {
        show: true,
        position: 'top',
        color: C.ink,
        fontFamily: 'GeistMono, monospace',
        formatter: '{c}%'
      },
      itemStyle: { borderRadius: [4, 4, 0, 0] }
    }]
  });

  /* ---------- 4. Timeliness Trend (line) ---------- */
  var timelinessChart = echarts.init(document.getElementById('chart-timeliness'));
  timelinessChart.setOption({
    animation: false,
    tooltip: Object.assign({}, tooltipBase(), { trigger: 'axis', formatter: function (p) { return p[0].name + '<br/>' + p[0].seriesName + '：' + p[0].value + '%'; } }),
    grid: { top: 36, left: 56, right: 24, bottom: 32 },
    xAxis: {
      type: 'category',
      data: ['第1周 (7.1-7.6)', '第2周 (7.7-7.13)', '第3周 (7.14-7.20)'],
      axisLine: axisLine(),
      axisLabel: textStyle(),
      axisTick: { show: false }
    },
    yAxis: {
      type: 'value',
      min: 92,
      max: 100,
      axisLabel: Object.assign({}, textStyle(), { formatter: '{value}%' }),
      axisLine: { show: false },
      splitLine: splitLine()
    },
    series: [
      {
        name: '时效达成率',
        type: 'line',
        smooth: true,
        symbol: 'circle',
        symbolSize: 10,
        lineStyle: { width: 3, color: C.accent },
        itemStyle: { color: C.accent, borderColor: C.bg2, borderWidth: 2 },
        areaStyle: {
          color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
            { offset: 0, color: 'rgba(56,189,248,0.30)' },
            { offset: 1, color: 'rgba(0,0,0,0)' }
          ])
        },
        label: {
          show: true,
          color: C.ink,
          fontFamily: 'GeistMono, monospace',
          fontSize: 13,
          formatter: '{c}%'
        },
        markLine: {
          silent: true,
          symbol: 'none',
          lineStyle: { color: '#f472b6', type: 'dashed', width: 1 },
          label: { formatter: '目标 95%', color: '#f472b6', fontFamily: 'InstrumentSans, sans-serif', fontSize: 11 },
          data: [{ yAxis: 95 }]
        },
        data: [94.2, 95.1, 96.8]
      }
    ]
  });

  /* ---------- 5. Exception Rate (gauge) ---------- */
  var gaugeChart = echarts.init(document.getElementById('chart-gauge'));
  gaugeChart.setOption({
    animation: false,
    series: [{
      type: 'gauge',
      radius: '90%',
      center: ['50%', '55%'],
      startAngle: 220,
      endAngle: -40,
      min: 0,
      max: 5,
      splitNumber: 5,
      axisLine: {
        lineStyle: {
          width: 18,
          color: [
            [0.36, C.accent],  /* 0-1.8 */
            [0.6, '#fb923c'],   /* 1.8-3 */
            [1, '#ef4444']     /* 3-5 */
          ]
        }
      },
      pointer: {
        icon: 'path://M12.8,0.7l12,40.1H0.7L12.8,0.7z',
        length: '55%',
        width: 10,
        offsetCenter: [0, '-10%'],
        itemStyle: { color: C.ink }
      },
      axisTick: { distance: -18, length: 6, lineStyle: { color: '#fff', width: 1.5 } },
      splitLine: { distance: -18, length: 18, lineStyle: { color: '#fff', width: 2 } },
      axisLabel: {
        color: C.muted,
        distance: 28,
        fontSize: 12,
        fontFamily: 'GeistMono, monospace'
      },
      anchor: { show: true, size: 16, itemStyle: { borderWidth: 3, borderColor: C.accent, color: C.bg2 } },
      title: { show: true, offsetCenter: [0, '30%'], color: C.muted, fontSize: 13, fontFamily: 'InstrumentSans, sans-serif' },
      detail: {
        valueAnimation: false,
        fontSize: 28,
        fontFamily: 'GeistMono, monospace',
        offsetCenter: [0, '55%'],
        color: C.accent,
        formatter: '{value}%'
      },
      data: [{ value: 1.8, name: '当前异常率 (目标 < 3%)' }]
    }]
  });

  /* ---------- 6. Utilization (horizontal bar) ---------- */
  var utilChart = echarts.init(document.getElementById('chart-util'));
  utilChart.setOption({
    animation: false,
    tooltip: Object.assign({}, tooltipBase(), { trigger: 'axis', formatter: function (p) { return p[0].name + '：' + p[0].value + '%'; } }),
    grid: { top: 8, left: 72, right: 56, bottom: 8 },
    xAxis: {
      type: 'value',
      max: 100,
      axisLabel: Object.assign({}, textStyle(), { formatter: '{value}%' }),
      axisLine: { show: false },
      splitLine: splitLine()
    },
    yAxis: {
      type: 'category',
      data: ['德仓', '英仓', '美东仓', '美西仓'],
      axisLine: axisLine(),
      axisLabel: textStyle(),
      axisTick: { show: false }
    },
    series: [{
      type: 'bar',
      barWidth: '52%',
      data: [
        { value: 71, itemStyle: { color: palette[3] } },
        { value: 68, itemStyle: { color: palette[2] } },
        { value: 75, itemStyle: { color: palette[1] } },
        { value: 82, itemStyle: { color: new echarts.graphic.LinearGradient(0, 0, 1, 0, [{ offset: 0, color: '#fb923c' }, { offset: 1, color: '#f97316' }]) } }
      ],
      label: {
        show: true,
        position: 'right',
        color: C.ink,
        fontFamily: 'GeistMono, monospace',
        fontSize: 13,
        formatter: '{c}%'
      },
      itemStyle: { borderRadius: [0, 4, 4, 0] },
      markLine: {
        silent: true,
        symbol: 'none',
        lineStyle: { color: '#ef4444', type: 'dashed', width: 1 },
        label: { formatter: '警戒线 85%', color: '#ef4444', fontFamily: 'InstrumentSans, sans-serif', fontSize: 11, position: 'insideEndTop' },
        data: [{ xAxis: 85 }]
      }
    }]
  });

  /* ---------- resize listener ---------- */
  var allCharts = [trendChart, pieChart, barChart, timelinessChart, gaugeChart, utilChart];
  var resizeTimer;
  window.addEventListener('resize', function () {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(function () {
      allCharts.forEach(function (c) { c.resize(); });
    }, 120);
  });

})();
